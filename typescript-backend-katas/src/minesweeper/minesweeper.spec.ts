
describe("Example test", () => {
  it("should sum numbers correctly", () => {
    expect(1 + 1).toBe(2);
  });
});


function generateGrid(row: number,
                      line: number,
                      mines: number[][]
) : number[][]{
  const result: number[1][1];
  return result;
}


describe("Example test", () => {
  it("should sum numbers correctly", () => {
    generateGrid(1, 1, [][]).toBe(2);
  });
});
