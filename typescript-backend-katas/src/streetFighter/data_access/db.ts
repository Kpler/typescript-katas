import { Match } from "../models/models";

export function getMatch(input: string): Match {
    const result = {
        fighter1: "chun-li",
        fighter2: "sagat",
        winner: "chun-li"
    }
    return result;
  }